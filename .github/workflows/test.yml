name: Test
on:
  pull_request:
  push:

jobs:
  test-migration:
    # Run workflows in fork on push, or in main repo on PR
    if: github.repository == 'ByteMyPawn/cantuscatholici-backend' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    env:
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      POSTGRES_USER: cantuscatholici
      POSTGRES_DB: cantuscatholici
      POSTGRES_HOST: localhost
      DOCKER_IMAGE: cantuscatholici-backend
      LOG_DB_ENGINE_ECHO: True
      APP_STATIC_CONTENT_PREFIX: /data/cantuscatholici/dummy
      AUTH_SECRET_KEY: ${{ secrets.AUTH_SECRET_KEY }}
      TAG: ${{ github.sha }}
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/project-setup
      - run: docker compose down -v --remove-orphans
      - name: Setup database service and wait for health
        run: |
          docker compose up -d db
          timeout 60 bash -c 'until docker compose exec -T db pg_isready -U cantuscatholici; do sleep 1; done'
      - name: Pytest migrations
        run: uv run pytest -m alembic
      - run: docker compose down -v --remove-orphans

  test:
    # Run workflows in fork on push, or in main repo on PR
    if: github.repository == 'ByteMyPawn/cantuscatholici-backend' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    needs: [test-migration]
    env:
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      POSTGRES_USER: cantuscatholici
      POSTGRES_DB: cantuscatholici
      POSTGRES_HOST: localhost
      DOCKER_IMAGE: cantuscatholici-backend
      LOG_DB_ENGINE_ECHO: True
      APP_STATIC_CONTENT_PREFIX: /data/cantuscatholici/dummy
      AUTH_SECRET_KEY: ${{ secrets.AUTH_SECRET_KEY }}
      TAG: ${{ github.sha }}
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/project-setup
      - run: docker compose down -v --remove-orphans
      - name: Setup database service and wait for health
        run: |
          docker compose up -d db
          timeout 60 bash -c 'until docker compose exec -T db pg_isready -U cantuscatholici; do sleep 1; done'
      - name: Pytest app
        run: uv run pytest -m "not alembic"
      - run: docker compose down -v --remove-orphans
